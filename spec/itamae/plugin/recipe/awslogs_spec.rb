require 'spec_helper'

describe Itamae::Plugin::Recipe::Awslogs do
  it 'has a version number' do
    expect(Itamae::Plugin::Recipe::Awslogs::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
